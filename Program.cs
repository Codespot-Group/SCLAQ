class Program
{
    static void Main(string[] args)
    {
        Console.WriteLine(AutoCodeTypeName.generate());
    }
}

/// 1 - Entender o CREATE TABLE
/// 2 - Gerar a lista de execução (sequência de inserts)
/// 3 - Exportar para o arquivo